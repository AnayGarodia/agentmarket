"""Implementation submodules for ``aztea.client``.

The public import path is ``aztea.client`` (or ``aztea.AzteaClient``); these
submodules are an internal split to keep individual files under the repo's
1000-line CI budget. Do not import from here directly — names may move.
"""
